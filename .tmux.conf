# Bind ctrl-x as prefix key
unbind C-b
set-option -g prefix C-x
bind-key C-x send-prefix
# Bind ctrl-k to clear terminal buffer
bind -n C-k clear-history

set -g mouse on

bind q killp
bind r source-file ~/.tmux.conf

# Loud or quiet?
set -g visual-activity off
set -g visual-bell off
set -g visual-silence off
setw -g monitor-activity off
set -g bell-action none
set -g allow-passthrough on

# Increase scrollback buffer size from 2000 to 50000 lines
set -g history-limit 50000

### Statusbar
set -g status-position bottom
set -g status-justify left
set -g status-right-length 100
set -g status-left-length 100
set -g status-interval 1
set -g @catppuccin_window_status_style "rounded"
set -g @catppuccin_window_text " #W"
set -g @catppuccin_window_current_text " #W"
set -g status-right "#[fg=#{@thm_crust},bg=#{@thm_red}]  #{cpu_percentage}  🐏#{ram_percentage} "
set -ag status-right "#[fg=#{@thm_crust},bg=#{@thm_teal}] 󰘚 #{gpu_percentage}  V🐏#{gram_percentage} "
set -ag status-right "#[fg=#{@thm_crust},bg=#{@thm_lavender}]  #H "
set -ag status-right "#[fg=#{@thm_crust},bg=#{@thm_blue}]  %a %b %d %H:%M%p "

set -as terminal-overrides ',xterm-kitty:RGB'

### Plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'jaclu/tmux-menus'
set -g @plugin 'b0o/tmux-autoreload'
set -g @plugin 'tmux-plugins/tmux-cowboy'
set -g @plugin 'tmux-plugins/tmux-logging'
set -g @plugin 'tmux-plugins/tmux-cpu'

# Theme
set -g @plugin 'catppuccin/tmux#v2.1.3'
set -g @catppuccin_flavor 'mocha'

if-shell '[ ! -z "$VIMRUNTIME" ]' { set -g status off }

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'

return {
    "danymat/neogen",
    config = true,
    -- automatic jump (with insert mode) on inserted annotation
    input_after_comment = true,
    -- Uncomment next line if you want to follow only stable versions
    -- version = "*" 
}
